import { Badge } from "@/components/ui/badge";
import { JOB_STATUSES } from "@/lib/constants";
export type ApplicationItem = { id: string; job: string; company: string; status: string; match: number };
export function ApplicationPipeline({ items }: { items: ApplicationItem[] }) { if (!items.length) return <div className="grid min-h-56 place-items-center rounded-xl border border-dashed border-border text-sm text-subtle">Nenhuma candidatura registrada.</div>; return <div className="overflow-hidden rounded-xl border border-border bg-surface"><div className="overflow-x-auto"><table className="w-full text-left"><thead><tr className="border-b border-border text-[10px] font-semibold uppercase tracking-wider text-subtle"><th className="px-5 py-3">Oportunidade</th><th className="px-4 py-3">Match</th><th className="px-4 py-3">Etapa atual</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-b border-border last:border-0"><td className="px-5 py-4"><p className="text-sm font-medium">{item.job}</p><p className="mt-1 text-xs text-muted-foreground">{item.company}</p></td><td className="px-4 font-mono text-sm text-accent">{item.match}%</td><td className="px-4"><Badge variant="neutral">{JOB_STATUSES.find((status) => status.value === item.status)?.label ?? item.status}</Badge></td></tr>)}</tbody></table></div></div>; }

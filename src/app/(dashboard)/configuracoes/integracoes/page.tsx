import { Bot, Check, Database, MessageCircle, PlugZap } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
export const metadata = { title: "Integrações" };
const integrations = [{ name: "PostgreSQL", description: "Persistência principal via Prisma ORM", icon: Database, active: true }, { name: "OpenAI / Anthropic / Gemini", description: "Provedores intercambiáveis pela camada lib/ai", icon: Bot, active: false }, { name: "WhatsApp Business Platform", description: "Integração oficial futura; hoje o envio é manual", icon: MessageCircle, active: false }];
export default function IntegrationsPage() { return <div className="space-y-6"><PageHeader eyebrow="Configurações" title="Integrações" description="Conecte apenas provedores oficiais. Segredos ficam em variáveis de ambiente e nunca chegam ao navegador." /><div className="grid gap-4 lg:grid-cols-2">{integrations.map(({ name, description, icon: Icon, active }) => <article key={name} className="flex items-start gap-4 rounded-xl border border-border bg-surface p-5"><span className="grid size-10 place-items-center rounded-lg border border-border bg-background"><Icon className="size-4 text-accent" /></span><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h2 className="text-sm font-medium">{name}</h2><Badge variant={active ? "success" : "neutral"}>{active ? "Configurado" : "Opcional"}</Badge></div><p className="mt-2 text-xs leading-5 text-muted-foreground">{description}</p><Button size="sm" variant="secondary" className="mt-4">{active ? <Check className="size-3.5" /> : <PlugZap className="size-3.5" />}{active ? "Verificar" : "Configurar"}</Button></div></article>)}</div></div>; }

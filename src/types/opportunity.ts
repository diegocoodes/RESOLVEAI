export type OpportunityJob = { id: string; title: string; company: string; location: string; mode: string; source: string; date: string; match: number; status: "Analisada" | "Currículo gerado" | "Salva"; skills: string[]; missing?: string[] };
export type OpportunityLead = { id: string; name: string; business: string; segment: string; location: string; state?: string; score: number; status: string; websiteStatus: "Sem site" | "Precisa melhorar" | "Bom site" | "Não verificado"; whatsapp: boolean; instagram: boolean; whatsappValue?: string; instagramValue?: string; phoneValue?: string; nextFollowUp?: string };

import { Check, Pencil, ShieldCheck } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";

export const metadata = { title: "Mensagens" };
const messages = [{ lead: "João Performance", text: "Olá, João! Conheci seu trabalho como personal em Recife e não encontrei um site próprio nas informações públicas consultadas...", status: "Aguardando revisão" }, { lead: "Clínica Vitta", text: "Olá, Marina! Conheci o trabalho da Clínica Vitta em Olinda e identifiquei alguns pontos que podem melhorar a experiência do site...", status: "Aprovada" }];
export default function MessagesPage() { return <div className="space-y-6"><PageHeader eyebrow="Prospecção" title="Mensagens" description="Revise abordagens geradas com dados verificáveis antes de abrir o canal de contato." /><div className="space-y-3">{messages.map((message) => <article key={message.lead} className="rounded-xl border border-border bg-surface p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div className="max-w-3xl"><div className="flex items-center gap-2"><h2 className="text-sm font-medium">{message.lead}</h2><Badge variant={message.status === "Aprovada" ? "success" : "warning"}>{message.status}</Badge></div><p className="mt-3 text-sm leading-6 text-muted-foreground">{message.text}</p></div><div className="flex shrink-0 items-start gap-2"><Button size="sm" variant="secondary"><Pencil className="size-3.5" />Editar</Button><Button size="sm"><Check className="size-3.5" />Aprovar</Button></div></div></article>)}</div><p className="inline-flex items-center gap-2 text-xs text-subtle"><ShieldCheck className="size-3.5" />O botão de WhatsApp aparece somente depois da aprovação e nunca confirma o envio pelo usuário.</p></div>; }

import Link from "next/link";
import { DatabaseZap, Plus, ShieldCheck } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { LeadSearchForm } from "@/features/leads/lead-search-form";

export const metadata = { title: "Buscar leads" };
export default function ProspectingPage() { return <div className="space-y-6"><PageHeader eyebrow="Prospecção" title="Buscar profissionais e negócios" description="Encontre nomes que contenham termos como personal, advogado ou nutri em qualquer cidade brasileira." actions={<Button asChild variant="secondary"><Link href="/leads/novo"><Plus className="size-4" />Cadastrar manualmente</Link></Button>} /><div className="grid gap-5 xl:grid-cols-[1.4fr_0.6fr]"><LeadSearchForm /><div className="space-y-5"><Card><CardHeader><div><h2 className="text-sm font-semibold">Fonte</h2><p className="mt-1 text-xs text-muted-foreground">Busca integrada</p></div><DatabaseZap className="size-4 text-accent" /></CardHeader><CardContent className="space-y-3"><Provider name="OpenStreetMap / Overpass" status="Dentro do app" /><p className="text-[11px] leading-5 text-subtle">A busca retorna somente registros cujo nome contém o texto pesquisado. Categorias relacionadas, como academias, não são incluídas automaticamente.</p></CardContent></Card><Card><CardHeader><h2 className="text-sm font-semibold">Uso responsável</h2></CardHeader><CardContent><div className="flex gap-3"><ShieldCheck className="mt-0.5 size-4 shrink-0 text-accent" /><p className="text-xs leading-5 text-muted-foreground">Revise cada resultado, registre a origem, respeite pedidos de não contato e consulte os <Link className="text-accent hover:underline" href="/termos">termos</Link> e a <Link className="text-accent hover:underline" href="/privacidade">privacidade</Link>.</p></div></CardContent></Card></div></div></div>; }
function Provider({ name, status }: { name: string; status: string }) { return <div className="flex items-center justify-between rounded-lg border border-border bg-background px-3 py-2.5 text-xs"><span>{name}</span><span className="text-emerald-400">{status}</span></div>; }
